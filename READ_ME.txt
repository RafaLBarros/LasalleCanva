É necessário configurar uma key do firebase firestore e apelidar de firebase-key.json na pasta raiz.
É necessário baixar node.js e baixar os módulos (npm init -y)
Além do node, é necessário baixar express. (npm install express firebase-admin cors)